e-shop desktop app
technologies: java, javafx, mysql

window examples in images

primary window of newest offers in database (viewable component amount is hardcoded atm)
second window of login/registration (autocheck for input chars, instant login option after registration)
dashboard with table and crud options. Responsive - viewable in small screens down to tablet size.

mvc, crud, validations, regex, image storage in db, admin/regular user options.

